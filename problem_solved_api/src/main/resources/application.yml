spring:
  application:
    name: leetcode-stats-api
  webflux:
    base-path: /api

server:
  port: 8080
  servlet:
    context-path: /api

logging:
  level:
    root: INFO
    com.marcus.leetcodestats: DEBUG
